export interface NavItem {
  label: string;
  href: string;
}

export interface Paper {
  title: string;
  category: string;
  date: string;
  abstract: string;
}

export interface StrategyPoint {
  title: string;
  value: string;
  description: string;
}